# ===================== 核心必配（适配你的ini-proc子包场景）=====================
# 忽略所有子目录/根目录下的debug编译目录（适配你的ini-proc/debug/）
**/debug/
# 忽略release发布版编译目录（后续打包会生成，提前忽略）
**/release/
# 忽略Cargo自动生成的target目录（标准Rust项目的编译产物目录，部分场景会生成）
**/target/

# ===================== Git自身缓存（可选，建议忽略）=====================
# Git的临时索引文件（偶尔生成，无提交价值）
*.gitindex
# Git的合并冲突临时文件
*.orig

# ===================== 通用开发文件（可选，按需保留）=====================
# 编辑器临时文件（vim/nano/VSCode）
*~
.*.swp
.DS_Store
.idea/
.vscode/
*.suo
*.sublime-*

# ===================== Rust进阶忽略（后续扩展项目会用到）=====================
# Cargo的本地配置（含镜像、本地构建参数，开发环境专属）
.cargo/
# 构建脚本生成的配置文件
**/build/
# Rust的锁文件（可选：Cargo.lock，库项目建议忽略，二进制项目可提交，下面注释了）
# Cargo.lock
# 依赖下载的缓存文件
**/vendor/
# 静态库/动态库文件
*.rlib
*.so
*.dll
*.dylib
*.rmeta
